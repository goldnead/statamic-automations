/**
 * Moved to `@goldnead/flow-canvas`.
 *
 * A pointer, not a copy — see the note in the package README. Import paths in
 * this addon keep working, and the code lives in one place.
 */

export * from '@goldnead/flow-canvas/composables/useHistory.js';
