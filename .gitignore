/vendor
/node_modules
composer.lock
# package-lock.json is COMMITTED — CI runs `npm ci` for reproducible dist builds.
.phpunit.cache
.phpunit.result.cache
.idea
.vscode
.DS_Store
*.log
.env

# Note: resources/dist/ (compiled CP assets) IS committed — Statamic ships and
# publishes it to the host's public/ on install, so there is no end-user build
# step. Do not ignore it.

# Codespaces playground (scaffolded Statamic site for addon dev)
/playground

# Interne Arbeitsunterlagen von Agentenlaeufen. Sie lagen bis 05.09.2026
# getrackt in diesem oeffentlichen Repo; progress.md enthielt einen
# Testbench-Zugang im Klartext. export-ignore in .gitattributes hielt sie nur
# aus dem Composer-Tarball heraus, nicht von GitHub.
/.superpowers
/docs/superpowers
