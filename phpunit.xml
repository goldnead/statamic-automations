<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         processIsolation="false"
         stopOnFailure="false"
         cacheDirectory=".phpunit.cache"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
        <testsuite name="Engine">
            <directory suffix="Test.php">./tests/Engine</directory>
        </testsuite>
        <!--
            The migration path itself, against tables that already hold rows.
            Every other suite meets a database RefreshDatabase has migrated to
            head; these install an earlier release, put data in and migrate
            forward. Present in both configurations on purpose — the failure
            that prompted them aborts with `duplicate column name: brand_id` on
            SQLite and ERROR 1060 on MySQL, and neither engine rolls the half
            state back.
        -->
        <testsuite name="Migrations">
            <directory suffix="Test.php">./tests/Migrations</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>./src</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
    </php>
</phpunit>
